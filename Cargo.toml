# This file was automatically generated.

[package]
edition = "2024"
name = "lode-stm32h723"
version = "0.1.0"

[dependencies]
cortex-m = { version = "0.7.7", features = ["inline-asm", "critical-section-single-core"] }
cortex-m-rt = "0.7.5"
defmt = { version = "1.0.1", optional = true }
defmt-rtt = { version = "1.1.0", optional = true }
embassy-futures = "0.1.2"
embassy-stm32 = { version = "0.6.0", features = ["memory-x", "stm32h723zg", "time-driver-any", "exti", "unstable-pac"] }
embassy-executor = { version = "0.9.1", features = ["arch-cortex-m", "executor-thread"] }
embassy-sync = "0.8.0"
embassy-time = { version = "0.5.1", features = ["tick-hz-32_768"] }
panic-halt = "1.0.0"
panic-probe = { version = "1.0.0", features = ["print-defmt"], optional = true }
embedded-hal = "1.0.0"
embassy-net = { version = "0.9.0", features = ["tcp", "dhcpv4", "dns", "medium-ethernet"] }
static_cell = "2.1.1"
heapless = "0.9.2"
embedded-io-async = "0.7.0"
reqwless = "0.14.0"
der = { version = "0.8.0", features = ["heapless"] }

[lib]
test = false
bench = false

[[bin]]
name = "hello"
path = "src/bin/hello.rs"
test = false
bench = false

[[bin]]
name = "bme280-test"
path = "src/bin/bme280-test.rs"
test = false
bench = false

[[bin]]
name = "nucleo"
path = "src/bin/nucleo.rs"
test = false
bench = false

[[bin]]
name = "ssd1306-test"
path = "src/bin/ssd1306-test.rs"
test = false
bench = false

[profile.dev]
debug = true
lto = false
opt-level = 1
incremental = true

# These crypto crates are slow to compile under any optimization.
[profile.dev.package.p384]
opt-level = 0

[profile.dev.package.p256]
opt-level = 0

[profile.dev.package.embedded-tls]
opt-level = 0

[profile.release]
debug = false
lto = true
opt-level = "z"
incremental = true

[features]
defmt = ["dep:defmt"]
defmt-rtt = ["dep:defmt-rtt"]
panic-probe = ["dep:panic-probe"]
default = ["debug"]
debug = [
    "defmt",
    "defmt-rtt",
    "panic-probe",
    "embassy-executor/defmt",
    "embassy-sync/defmt",
    "embassy-futures/defmt",
    "embassy-time/defmt",
    "embassy-time/defmt-timestamp-uptime",
    "embassy-stm32/defmt",
]
